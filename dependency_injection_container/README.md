## Dependency Injection Container
Based on "dependency injection" example `a005_step_04.py`

Key Characteristics of DI Container:
- `register()`
- `resolve()`

### Step 1
add 
- `Container` class
- `register` method
- `resolve` method with singleton logic 

### Step 2
TAKEAWAY: Creating object is much easier BUT the reason this approach is interesting
because it gives you more flexibility where you do this.
